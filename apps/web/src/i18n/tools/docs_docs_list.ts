// i18n catalogue for `mcp-vertex_docs_docs_list`.

import type { IToolI18n } from '#I18N/tools/_shape';

export const docsDocsListI18n: IToolI18n = {
	description: {
		en: 'List the project documentation: markdown under the configured roots as {path, title}. Low-token index — read one with docs_read. Paginated: `limit` (default 50, max 200) + `offset`; returns {docs,count,total,offset,nextOffset?,truncated}. Read-only.',
		es: 'Lista la documentación del proyecto: markdown bajo las raíces configuradas como {path, title}. Índice de pocos tokens — lee uno con docs_read. Paginado: `limit` (por defecto 50, máx 200) + `offset`; devuelve {docs,count,total,offset,nextOffset?,truncated}. Solo lectura.',
		fr: 'Liste la documentation du projet : markdown sous les racines configurées sous forme de {path, title}. Index à faible coût en tokens — lisez-en un avec docs_read. Pagination : `limit` (défaut 50, max 200) + `offset` ; renvoie {docs,count,total,offset,nextOffset?,truncated}. Lecture seule.',
		de: 'Listet die Projektdokumentation auf: Markdown unter den konfigurierten Wurzeln als {path, title}. Token-armer Index — lies eine mit docs_read. Paginiert: `limit` (Standard 50, max 200) + `offset`; gibt {docs,count,total,offset,nextOffset?,truncated} zurück. Nur lesend.',
		it: 'Elenca la documentazione del progetto: markdown sotto le radici configurate come {path, title}. Indice a basso consumo di token — leggine uno con docs_read. Paginato: `limit` (predefinito 50, max 200) + `offset`; restituisce {docs,count,total,offset,nextOffset?,truncated}. Solo lettura.',
		pt: 'Lista a documentação do projeto: markdown sob as raízes configuradas como {path, title}. Índice de baixo consumo de tokens — leia um com docs_read. Paginado: `limit` (padrão 50, máx 200) + `offset`; devolve {docs,count,total,offset,nextOffset?,truncated}. Apenas leitura.',
		ja: 'プロジェクトドキュメントを一覧表示します:設定されたルート下の markdown を {path, title} として返します。低トークンのインデックス — 個別の内容は docs_read で読みます。ページネーション:`limit`(デフォルト 50、最大 200)+ `offset`;{docs,count,total,offset,nextOffset?,truncated} を返します。読み取り専用。',
		zh: '列出项目文档:配置根目录下的 markdown 文件,以 {path, title} 形式呈现。低令牌索引——使用 docs_read 读取具体内容。支持分页:`limit`(默认 50,最大 200)+ `offset`;返回 {docs,count,total,offset,nextOffset?,truncated}。只读。',
		hi: 'प्रोजेक्ट दस्तावेज़ीकरण सूचीबद्ध करता है: कॉन्फ़िगर की गई रूट्स के अंतर्गत markdown को {path, title} के रूप में। कम-टोकन इंडेक्स — किसी एक को docs_read से पढ़ें। पेजिनेटेड: `limit` (डिफ़ॉल्ट 50, अधिकतम 200) + `offset`; {docs,count,total,offset,nextOffset?,truncated} लौटाता है। केवल पढ़ने के लिए।',
		ar: 'يسرد توثيق المشروع: ملفات markdown تحت الجذور المُهيّأة بصيغة {path, title}. فهرس منخفض الرموز — اقرأ واحدًا باستخدام docs_read. مُرقّم صفحاتٍ: `limit` (الافتراضي 50، الحد الأقصى 200) + `offset`؛ يُرجع {docs,count,total,offset,nextOffset?,truncated}. للقراءة فقط.',
		th: 'แสดงรายการเอกสารของโปรเจกต์: markdown ภายใต้รูทที่กำหนดในรูปแบบ {path, title} ดัชนีที่ใช้โทเค็นต่ำ — อ่านแต่ละรายการด้วย docs_read แบ่งหน้า: `limit` (ค่าเริ่มต้น 50 สูงสุด 200) + `offset`; ส่งคืน {docs,count,total,offset,nextOffset?,truncated} อ่านอย่างเดียว',
		vi: 'Liệt kê tài liệu của dự án: markdown dưới các thư mục gốc đã cấu hình dưới dạng {path, title}. Chỉ mục ít token — đọc một tài liệu cụ thể bằng docs_read. Có phân trang: `limit` (mặc định 50, tối đa 200) + `offset`; trả về {docs,count,total,offset,nextOffset?,truncated}. Chỉ đọc.',
	},
};
