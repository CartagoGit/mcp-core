// i18n catalogue for `mcp-vertex_git_diff`.

import type { IToolI18n } from '#I18N/tools/_shape';

export const gitDiffI18n: IToolI18n = {
	description: {
		en: 'Returns `git diff --stat` (file/line change summary). Optionally staged-only or scoped to a path. Low-token; read-only.',
		es: 'Devuelve `git diff --stat` (resumen de cambios por fichero/línea). Opcionalmente solo staged o limitado a una ruta. Pocos tokens; solo lectura.',
		fr: 'Renvoie `git diff --stat` (résumé des changements par fichier/ligne). Optionnellement limité aux fichiers staged ou à un chemin. Peu de tokens ; lecture seule.',
		de: 'Gibt `git diff --stat` zurück (Zusammenfassung der Datei-/Zeilenänderungen). Optional nur staged oder auf einen Pfad beschränkt. Wenig Tokens; nur lesend.',
		it: 'Restituisce `git diff --stat` (riepilogo delle modifiche per file/riga). Opzionalmente solo staged o limitato a un percorso. Pochi token; solo lettura.',
		pt: 'Devolve `git diff --stat` (resumo das alterações por ficheiro/linha). Opcionalmente apenas staged ou limitado a um caminho. Poucos tokens; apenas leitura.',
		ja: '`git diff --stat`(ファイル/行ごとの変更サマリー)を返します。任意でステージ済みのみ、またはパスに限定できます。低トークン;読み取り専用。',
		zh: '返回 `git diff --stat`(文件/行变更摘要)。可选仅限已暂存或限定于某路径。低令牌;只读。',
		hi: '`git diff --stat` लौटाता है (फ़ाइल/लाइन परिवर्तन सारांश)। वैकल्पिक रूप से केवल staged या किसी पथ तक सीमित। कम टोकन; केवल पढ़ने के लिए।',
		ar: 'يُرجع `git diff --stat` (ملخص تغييرات الملفات/الأسطر). اختياريًا للملفات المرحلية (staged) فقط أو محدد بمسار. رموز قليلة؛ للقراءة فقط.',
		th: 'ส่งคืน `git diff --stat` (สรุปการเปลี่ยนแปลงไฟล์/บรรทัด) เลือกได้ว่าจะเฉพาะ staged หรือจำกัดเฉพาะพาธ โทเค็นน้อย อ่านอย่างเดียว',
		vi: 'Trả về `git diff --stat` (tóm tắt thay đổi theo tệp/dòng). Có thể chỉ giới hạn ở staged hoặc một đường dẫn cụ thể. Ít token; chỉ đọc.',
	},
};
