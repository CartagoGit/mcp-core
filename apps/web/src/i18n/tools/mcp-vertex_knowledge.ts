// i18n catalogue for `mcp-vertex_knowledge`.

import type { IToolI18n } from '#I18N/tools/_shape';

export const mcpVertexKnowledgeI18n: IToolI18n = {
	description: {
		en: 'Access plugin knowledge on demand. Without `id`: list every entry as {id,title}. With `id`: return that entry. Read-only and low-token (fetch only what you need).',
		es: 'Accede al conocimiento de los plugins a demanda. Sin `id`: lista todas las entradas como {id,title}. Con `id`: devuelve esa entrada. Solo lectura y pocos tokens (recupera solo lo que necesitas).',
		fr: 'Accède aux connaissances des plugins à la demande. Sans `id` : liste chaque entrée sous forme de {id,title}. Avec `id` : renvoie cette entrée. Lecture seule et peu de tokens (récupérez seulement ce dont vous avez besoin).',
		de: 'Greift bedarfsgerecht auf Plugin-Wissen zu. Ohne `id`: listet jeden Eintrag als {id,title} auf. Mit `id`: gibt diesen Eintrag zurück. Nur lesend und token-arm (hole nur, was du brauchst).',
		it: 'Accede alla conoscenza dei plugin su richiesta. Senza `id`: elenca ogni voce come {id,title}. Con `id`: restituisce quella voce. Solo lettura e a basso consumo di token (recupera solo ciò di cui hai bisogno).',
		pt: 'Acede ao conhecimento dos plugins a pedido. Sem `id`: lista todas as entradas como {id,title}. Com `id`: devolve essa entrada. Apenas leitura e baixo consumo de tokens (obtém apenas o que precisas).',
		ja: 'プラグインのナレッジに必要に応じてアクセスします。`id` なし:すべてのエントリを {id,title} として一覧表示します。`id` あり:そのエントリを返します。読み取り専用かつ低トークン(必要なものだけ取得)。',
		zh: '按需访问插件知识。不带 `id`:以 {id,title} 形式列出每个条目。带 `id`:返回该条目。只读且低令牌(只获取你需要的内容)。',
		hi: 'मांग पर प्लगइन ज्ञान तक पहुँचता है। `id` के बिना: हर एंट्री को {id,title} के रूप में सूचीबद्ध करता है। `id` के साथ: वह एंट्री लौटाता है। केवल पढ़ने के लिए और कम-टोकन (केवल वही प्राप्त करें जिसकी आवश्यकता है)।',
		ar: 'يصل إلى معرفة الإضافات عند الطلب. بدون `id`: يسرد كل إدخال بصيغة {id,title}. مع `id`: يُرجع ذلك الإدخال. للقراءة فقط ومنخفض الرموز (احصل فقط على ما تحتاجه).',
		th: 'เข้าถึงความรู้ของปลั๊กอินตามต้องการ ไม่มี `id`: แสดงรายการทุกเอนทรีในรูปแบบ {id,title} มี `id`: ส่งคืนเอนทรีนั้น อ่านอย่างเดียวและใช้โทเค็นต่ำ (ดึงเฉพาะสิ่งที่คุณต้องการ)',
		vi: 'Truy cập kiến thức của plugin theo nhu cầu. Không có `id`: liệt kê mọi mục dưới dạng {id,title}. Có `id`: trả về mục đó. Chỉ đọc và ít token (chỉ lấy những gì bạn cần).',
	},
};
