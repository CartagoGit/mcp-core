// i18n catalogue for `mcp-vertex_logs_query`.

import type { IToolI18n } from '#I18N/tools/_shape';

export const logsQueryI18n: IToolI18n = {
	description: {
		en: 'Query redacted append-only MCP log events. Filters: since, until, kind, agent, taskId, outcome; supports cursor pagination.',
		es: 'Consulta eventos de log MCP redactados de tipo append-only. Filtros: since, until, kind, agent, taskId, outcome; soporta paginación por cursor.',
		fr: 'Interroge les événements de log MCP append-only et expurgés. Filtres : since, until, kind, agent, taskId, outcome ; prend en charge la pagination par curseur.',
		de: 'Fragt geschwärzte Append-only-MCP-Logereignisse ab. Filter: since, until, kind, agent, taskId, outcome; unterstützt Cursor-Paginierung.',
		it: 'Interroga gli eventi di log MCP append-only e redatti. Filtri: since, until, kind, agent, taskId, outcome; supporta la paginazione tramite cursore.',
		pt: 'Consulta eventos de log MCP append-only e redigidos. Filtros: since, until, kind, agent, taskId, outcome; suporta paginação por cursor.',
		ja: '機密情報をマスクした追記専用の MCP ログイベントを照会します。フィルター:since, until, kind, agent, taskId, outcome;カーソルベースのページネーションをサポートします。',
		zh: '查询经过脱敏处理的仅追加 MCP 日志事件。过滤条件:since、until、kind、agent、taskId、outcome;支持基于游标的分页。',
		hi: 'रिडैक्टेड अपेंड-ओनली MCP लॉग इवेंट्स को क्वेरी करता है। फ़िल्टर: since, until, kind, agent, taskId, outcome; कर्सर पेजिनेशन समर्थित है।',
		ar: 'يستعلم عن أحداث سجل MCP المنقّحة والمضافة فقط (append-only). المرشحات: since, until, kind, agent, taskId, outcome؛ يدعم الترقيم بالمؤشر (cursor).',
		th: 'ค้นหาเหตุการณ์บันทึก MCP แบบ append-only ที่ปกปิดข้อมูลลับแล้ว ตัวกรอง: since, until, kind, agent, taskId, outcome รองรับการแบ่งหน้าด้วยเคอร์เซอร์',
		vi: 'Truy vấn các sự kiện log MCP dạng append-only đã được ẩn thông tin nhạy cảm. Bộ lọc: since, until, kind, agent, taskId, outcome; hỗ trợ phân trang theo con trỏ (cursor).',
	},
};
