// i18n catalogue for `mcp-vertex_scaffold`.

import type { IToolI18n } from '#I18N/tools/_shape';

export const mcpVertexScaffoldI18n: IToolI18n = {
	description: {
		en: 'Generate host artefacts from mcp-vertex templates: a new tool, prompt, skill, agent adapter, or the complete host project (server, host config, orchestrator and subagents). Dry-run by default; writes skip existing files unless keepLegacy moves them under legacy/ first.',
		es: 'Genera artefactos de host a partir de plantillas de mcp-vertex: una nueva herramienta, prompt, skill, adaptador de agente, o el proyecto host completo (servidor, configuración de host, orquestador y subagentes). Dry-run por defecto; las escrituras omiten ficheros existentes a menos que keepLegacy los mueva primero bajo legacy/.',
		fr: "Génère des artefacts hôte à partir de modèles mcp-vertex : un nouvel outil, prompt, skill, adaptateur d'agent, ou le projet hôte complet (serveur, configuration hôte, orchestrateur et sous-agents). Dry-run par défaut ; les écritures ignorent les fichiers existants sauf si keepLegacy les déplace d'abord sous legacy/.",
		de: 'Generiert Host-Artefakte aus mcp-vertex-Vorlagen: ein neues Werkzeug, Prompt, Skill, Agent-Adapter oder das vollständige Host-Projekt (Server, Host-Konfiguration, Orchestrator und Subagenten). Standardmäßig Dry-Run; Schreibvorgänge überspringen vorhandene Dateien, sofern keepLegacy sie nicht zuerst unter legacy/ verschiebt.',
		it: 'Genera artefatti host da template mcp-vertex: un nuovo strumento, prompt, skill, adattatore agente, o l intero progetto host (server, configurazione host, orchestratore e subagenti). Dry-run di default; le scritture saltano i file esistenti a meno che keepLegacy non li sposti prima sotto legacy/.',
		pt: 'Gera artefactos de host a partir de templates do mcp-vertex: uma nova ferramenta, prompt, skill, adaptador de agente, ou o projeto host completo (servidor, configuração de host, orquestrador e subagentes). Dry-run por defeito; as escritas ignoram ficheiros existentes a menos que keepLegacy os mova primeiro para legacy/.',
		ja: 'mcp-vertex テンプレートからホストアーティファクトを生成します:新しいツール、プロンプト、スキル、エージェントアダプター、または完全なホストプロジェクト(サーバー、ホスト設定、オーケストレーター、サブエージェント)。デフォルトはドライラン;keepLegacy が先に legacy/ 配下へ移動しない限り、書き込みは既存ファイルをスキップします。',
		zh: '从 mcp-vertex 模板生成宿主工件:新工具、提示词、技能、代理适配器,或完整的宿主项目(服务器、宿主配置、编排器和子代理)。默认为演练模式(dry-run);除非 keepLegacy 先将现有文件移到 legacy/ 下,写入操作会跳过已存在的文件。',
		hi: 'mcp-vertex टेम्प्लेट्स से होस्ट आर्टिफैक्ट्स जनरेट करता है: एक नया टूल, प्रॉम्प्ट, स्किल, एजेंट एडाप्टर, या संपूर्ण होस्ट प्रोजेक्ट (सर्वर, होस्ट कॉन्फ़िग, ऑर्केस्ट्रेटर और सबएजेंट्स)। डिफ़ॉल्ट रूप से ड्राई-रन; जब तक keepLegacy पहले उन्हें legacy/ के अंतर्गत न ले जाए, लेखन मौजूदा फ़ाइलों को छोड़ देता है।',
		ar: 'يولّد قطعًا للمضيف (host artefacts) من قوالب mcp-vertex: أداة جديدة، موجه (prompt)، مهارة (skill)، محول وكيل، أو مشروع المضيف الكامل (الخادم، تهيئة المضيف، المنسق والوكلاء الفرعيين). تشغيل تجريبي (dry-run) بشكل افتراضي؛ تتجاهل الكتابة الملفات الموجودة إلا إذا قام keepLegacy بنقلها أولاً تحت legacy/.',
		th: 'สร้างอาร์ติแฟกต์โฮสต์จากเทมเพลตของ mcp-vertex: เครื่องมือใหม่ พรอมต์ สกิล อะแดปเตอร์เอเจนต์ หรือโปรเจกต์โฮสต์แบบสมบูรณ์ (เซิร์ฟเวอร์ การกำหนดค่าโฮสต์ ออร์เคสเตรเตอร์ และซับเอเจนต์) เป็น dry-run โดยค่าเริ่มต้น การเขียนจะข้ามไฟล์ที่มีอยู่แล้ว เว้นแต่ keepLegacy จะย้ายไฟล์เหล่านั้นไปไว้ใต้ legacy/ ก่อน',
		vi: 'Tạo các artefact host từ template của mcp-vertex: một công cụ mới, prompt, skill, adapter agent, hoặc toàn bộ dự án host (server, cấu hình host, orchestrator và subagent). Mặc định là dry-run; việc ghi sẽ bỏ qua các tệp đã tồn tại trừ khi keepLegacy di chuyển chúng vào legacy/ trước.',
	},
};
