// i18n catalogue for `mcp-vertex_memory_list`.

import type { IToolI18n } from '#I18N/tools/_shape';

export const memoryListI18n: IToolI18n = {
	description: {
		en: 'List notes as {id,title,tags}, newest first. Paginated: `limit` (default 50, max 200) + `offset`. Returns {notes,total,offset,nextOffset}. Read a body with mcp-vertex_memory_recall.',
		es: 'Lista las notas como {id,title,tags}, las más recientes primero. Paginado: `limit` (por defecto 50, máx 200) + `offset`. Devuelve {notes,total,offset,nextOffset}. Lee un cuerpo con mcp-vertex_memory_recall.',
		fr: 'Liste les notes sous forme de {id,title,tags}, les plus récentes en premier. Pagination : `limit` (défaut 50, max 200) + `offset`. Renvoie {notes,total,offset,nextOffset}. Lisez un corps avec mcp-vertex_memory_recall.',
		de: 'Listet Notizen als {id,title,tags} auf, neueste zuerst. Paginiert: `limit` (Standard 50, max 200) + `offset`. Gibt {notes,total,offset,nextOffset} zurück. Lies einen Inhalt mit mcp-vertex_memory_recall.',
		it: 'Elenca le note come {id,title,tags}, le più recenti prima. Paginato: `limit` (predefinito 50, max 200) + `offset`. Restituisce {notes,total,offset,nextOffset}. Leggi un corpo con mcp-vertex_memory_recall.',
		pt: 'Lista as notas como {id,title,tags}, mais recentes primeiro. Paginado: `limit` (padrão 50, máx 200) + `offset`. Devolve {notes,total,offset,nextOffset}. Lê o corpo com mcp-vertex_memory_recall.',
		ja: 'ノートを {id,title,tags} として、新しい順に一覧表示します。ページネーション:`limit`(デフォルト 50、最大 200)+ `offset`。{notes,total,offset,nextOffset} を返します。本文は mcp-vertex_memory_recall で読みます。',
		zh: '以 {id,title,tags} 形式列出笔记,最新的在前。支持分页:`limit`(默认 50,最大 200)+ `offset`。返回 {notes,total,offset,nextOffset}。使用 mcp-vertex_memory_recall 读取正文内容。',
		hi: 'नोट्स को {id,title,tags} के रूप में सूचीबद्ध करता है, सबसे नया पहले। पेजिनेटेड: `limit` (डिफ़ॉल्ट 50, अधिकतम 200) + `offset`। {notes,total,offset,nextOffset} लौटाता है। मुख्य सामग्री mcp-vertex_memory_recall से पढ़ें।',
		ar: 'يسرد الملاحظات بصيغة {id,title,tags}، الأحدث أولاً. مُرقّم صفحاتٍ: `limit` (الافتراضي 50، الحد الأقصى 200) + `offset`. يُرجع {notes,total,offset,nextOffset}. اقرأ المحتوى الكامل باستخدام mcp-vertex_memory_recall.',
		th: 'แสดงรายการโน้ตในรูปแบบ {id,title,tags} ใหม่สุดก่อน แบ่งหน้า: `limit` (ค่าเริ่มต้น 50 สูงสุด 200) + `offset` ส่งคืน {notes,total,offset,nextOffset} อ่านเนื้อหาด้วย mcp-vertex_memory_recall',
		vi: 'Liệt kê các ghi chú dưới dạng {id,title,tags}, mới nhất trước. Có phân trang: `limit` (mặc định 50, tối đa 200) + `offset`. Trả về {notes,total,offset,nextOffset}. Đọc nội dung bằng mcp-vertex_memory_recall.',
	},
};
