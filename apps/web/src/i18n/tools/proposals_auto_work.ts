// i18n catalogue for `proposals_auto_work`.
//
// Second entry of the per-tool i18n catalogue (apps/web/src/i18n/tools/).
// 12-lang invariant is enforced by `check-i18n.ts` (p100 s3-bis).

import type { IToolI18n } from './_shape';

export const proposalsAutoWorkI18n: IToolI18n = {
	description: {
		en: 'One call → what to do now. Resolves the next proposal (serial cascade) and returns a compact ordered plan (claim → slice → validate → sync → release), or an explicit idle state. Low-token: a tight action list, not prose.',
		es: 'Una llamada → qué hacer ahora. Resuelve la siguiente propuesta (cascada serial) y devuelve un plan ordenado compacto (reclamar → fragmento → validar → sincronizar → liberar), o un estado idle explícito. Pocos tokens: una lista de acciones, no prosa.',
		fr: 'Un appel → quoi faire maintenant. Résout la proposition suivante (cascade en série) et renvoie un plan ordonné compact (revendiquer → tranche → valider → synchroniser → libérer), ou un état idle explicite. Peu de tokens : une liste d actions, pas de prose.',
		de: 'Ein Aufruf → was jetzt zu tun ist. Löst den nächsten Vorschlag auf (serielle Kaskade) und gibt einen kompakten Aktionsplan zurück (beanspruchen → Scheibe → validieren → synchronisieren → freigeben) oder einen expliziten Leerlaufzustand. Wenig Tokens: eine enge Aktionsliste, keine Prosa.',
		it: 'Una chiamata → cosa fare ora. Risolve la proposta successiva (cascata seriale) e restituisce un piano ordinato compatto (rivendicare → slice → validare → sincronizzare → rilasciare), o uno stato idle esplicito. Pochi token: un elenco stretto di azioni, non prosa.',
		pt: 'Uma chamada → o que fazer agora. Resolve a próxima proposta (cascata serial) e devolve um plano ordenado compacto (reivindicar → fatia → validar → sincronizar → libertar), ou um estado idle explícito. Poucos tokens: uma lista apertada de ações, não prosa.',
		ja: '1 回の呼び出しで次に何をすべきかを解決します。次の提案(直列カスケード)を解決し、コンパクトな順序付きプラン(要求 → スライス → 検証 → 同期 → 解放)を返すか、明示的なアイドル状態を返します。少量トークン:散文ではなく、簡潔なアクションリスト。',
		zh: '一次调用 → 现在该做什么。解析下一个提案(串行级联)并返回紧凑的有序计划(声明 → 切片 → 验证 → 同步 → 释放),或显式的空闲状态。低令牌:紧凑的动作列表,而不是散文。',
		hi: 'एक कॉल → अब क्या करना है। अगला प्रस्ताव(सीरियल कैस्केड) हल करता है और एक कॉम्पैक्ट क्रमबद्ध योजना लौटाता है(दावा → स्लाइस → मान्य → सिंक → रिलीज़), या स्पष्ट निष्क्रिय स्थिति। कम टोकन:कसी हुई क्रिया सूची, गद्य नहीं।',
		ar: 'مكالمة واحدة → ما يجب فعله الآن. يحل الاقتراح التالي(تتابع تسلسلي) ويُرجع خطة مرتبة مضغوطة(مطالبة → شريحة → تحقق → مزامنة → تحرير)، أو حالة خمول صريحة. رمز قليل:قائمة إجراءات ضيقة، ليست نثرًا.',
		th: 'การเรียกหนึ่งครั้ง → ต้องทำอะไรตอนนี้ แก้ไขข้อเสนอถัดไป(น้ำตกแบบอนุกรม) และส่งคืนแผนการเรียงลำดับแบบกะทัดรัด(อ้างสิทธิ์ → สไลซ์ → ตรวจสอบ → ซิงค์ → ปล่อย) หรือสถานะว่างที่ชัดเจน โทเค็นน้อย:รายการการกระทำที่กระชับ ไม่ใช่ร้อยแก้ว',
		vi: 'Một cuộc gọi → cần làm gì bây giờ. Giải quyết đề xuất tiếp theo (chuỗi nối tiếp) và trả về một kế hoạch có thứ tự gọn gàng (nhận → lát → xác thực → đồng bộ → phát hành), hoặc trạng thái nhàn rỗi rõ ràng. Ít token: danh sách hành động chặt chẽ, không phải văn xuôi.',
	},
};
