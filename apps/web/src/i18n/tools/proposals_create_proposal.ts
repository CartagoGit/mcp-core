// i18n catalogue for `proposals_create_proposal`.

import type { IToolI18n } from '#I18N/tools/_shape';

export const proposalsCreateProposalI18n: IToolI18n = {
	description: {
		en: 'Create a proposal document with frontmatter, a Goal and a parseable `## Slices` section (one slice per parallelisable, file-disjoint unit). Validates disjointness, writes atomically and re-syncs the index. Returns the file path and any overlap issues.',
		es: 'Crea un documento de propuesta con frontmatter, un Goal y una sección `## Slices` parseable (un slice por unidad paralelizable y disjunta en ficheros). Valida la disjunción, escribe de forma atómica y resincroniza el índice. Devuelve la ruta del fichero y cualquier problema de solapamiento.',
		fr: "Crée un document de proposition avec frontmatter, un Goal et une section `## Slices` analysable (une tranche par unité parallélisable et disjointe en fichiers). Valide la disjonction, écrit de manière atomique et resynchronise l'index. Renvoie le chemin du fichier et tout problème de chevauchement.",
		de: 'Erstellt ein Vorschlagsdokument mit Frontmatter, einem Goal und einem parsbaren `## Slices`-Abschnitt (eine Slice pro parallelisierbarer, dateidisjunkter Einheit). Validiert die Disjunktheit, schreibt atomar und synchronisiert den Index neu. Gibt den Dateipfad und etwaige Überlappungsprobleme zurück.',
		it: 'Crea un documento di proposta con frontmatter, un Goal e una sezione `## Slices` analizzabile (una slice per unità parallelizzabile e disgiunta nei file). Valida la disgiunzione, scrive in modo atomico e ri-sincronizza l indice. Restituisce il percorso del file ed eventuali problemi di sovrapposizione.',
		pt: 'Cria um documento de proposta com frontmatter, um Goal e uma secção `## Slices` analisável (uma slice por unidade paralelizável e disjunta em ficheiros). Valida a disjunção, escreve atomicamente e re-sincroniza o índice. Devolve o caminho do ficheiro e quaisquer problemas de sobreposição.',
		ja: 'frontmatter、Goal、解析可能な `## Slices` セクション(並列化可能でファイルが重複しない単位ごとに 1 スライス)を持つ提案文書を作成します。重複しないことを検証し、アトミックに書き込み、インデックスを再同期します。ファイルパスと重複の問題があればそれを返します。',
		zh: '创建带有 frontmatter、目标(Goal)和可解析的 `## Slices` 区段(每个可并行、文件不重叠的单元一个切片)的提案文档。验证不重叠性,原子方式写入,并重新同步索引。返回文件路径以及任何重叠问题。',
		hi: 'फ्रंटमैटर, एक Goal, और एक पार्स करने योग्य `## Slices` सेक्शन (प्रत्येक पैरेललाइज़ेबल, फ़ाइल-डिसजॉइंट यूनिट के लिए एक स्लाइस) के साथ एक प्रस्ताव दस्तावेज़ बनाता है। डिसजॉइंटनेस को सत्यापित करता है, परमाणु रूप से लिखता है, और इंडेक्स को रि-सिंक करता है। फ़ाइल पथ और किसी भी ओवरलैप समस्या को लौटाता है।',
		ar: 'يُنشئ وثيقة اقتراح مع frontmatter وGoal وقسم `## Slices` قابل للتحليل (شريحة واحدة لكل وحدة قابلة للتوازي ومنفصلة في الملفات). يتحقق من عدم التداخل، يكتب بشكل ذري ويعيد مزامنة الفهرس. يُرجع مسار الملف وأي مشاكل تداخل.',
		th: 'สร้างเอกสารข้อเสนอที่มี frontmatter, Goal และส่วน `## Slices` ที่แยกวิเคราะห์ได้ (หนึ่งสไลซ์ต่อหน่วยที่ทำงานพร้อมกันได้และไม่ใช้ไฟล์ร่วมกัน) ตรวจสอบความไม่ทับซ้อน เขียนแบบอะตอมมิก และซิงค์ดัชนีใหม่ ส่งคืนพาธไฟล์และปัญหาการทับซ้อนใดๆ',
		vi: 'Tạo một văn bản đề xuất với frontmatter, một Goal và một phần `## Slices` có thể phân tích được (mỗi lát là một đơn vị có thể chạy song song, không trùng tệp với lát khác). Xác thực tính không trùng lặp, ghi theo kiểu atomic và đồng bộ lại chỉ mục. Trả về đường dẫn tệp và bất kỳ vấn đề chồng chéo nào.',
	},
};
