// i18n catalogue for `test-convention_suggest_spec_path`.

import type { IToolI18n } from '#I18N/tools/_shape';

export const testConventionSuggestSpecPathI18n: IToolI18n = {
	description: {
		en: 'For a given source file path inside the workspace, return the companion spec path the convention mandates (placement depends on `specLayout`) plus a minimal `describe(...)` skeleton the agent can paste as the spec body.',
		es: 'Para una ruta de fichero fuente dada dentro del workspace, devuelve la ruta del spec compañero que la convención exige (la ubicación depende de `specLayout`) más un esqueleto mínimo de `describe(...)` que el agente puede pegar como cuerpo del spec.',
		fr: "Pour un chemin de fichier source donné dans l'espace de travail, renvoie le chemin du spec compagnon exigé par la convention (l'emplacement dépend de `specLayout`) plus un squelette `describe(...)` minimal que l'agent peut coller comme corps du spec.",
		de: 'Für einen gegebenen Quelldateipfad im Workspace wird der von der Konvention vorgeschriebene zugehörige Spec-Pfad zurückgegeben (Platzierung abhängig von `specLayout`) sowie ein minimales `describe(...)`-Gerüst, das der Agent als Spec-Body einfügen kann.',
		it: 'Per un dato percorso di file sorgente all interno del workspace, restituisce il percorso dello spec companion richiesto dalla convenzione (il posizionamento dipende da `specLayout`) più uno scheletro minimo `describe(...)` che l agente può incollare come corpo dello spec.',
		pt: 'Para um determinado caminho de ficheiro fonte dentro do workspace, devolve o caminho do spec companion exigido pela convenção (a colocação depende de `specLayout`) mais um esqueleto mínimo `describe(...)` que o agente pode colar como corpo do spec.',
		ja: 'workspace 内の指定されたソースファイルパスに対して、規約が義務付ける対応する spec パス(配置は `specLayout` に依存)と、エージェントが spec の本体として貼り付けられる最小限の `describe(...)` スケルトンを返します。',
		zh: '对于工作区内给定的源文件路径,返回约定所规定的对应 spec 文件路径(具体位置取决于 `specLayout`),以及一个代理可以粘贴作为 spec 主体的最简 `describe(...)` 骨架。',
		hi: 'वर्कस्पेस के अंदर किसी दिए गए सोर्स फ़ाइल पथ के लिए, परंपरा द्वारा अनिवार्य कंपेनियन स्पेक पथ लौटाता है (स्थान `specLayout` पर निर्भर करता है) साथ ही एक न्यूनतम `describe(...)` स्केलेटन जिसे एजेंट स्पेक बॉडी के रूप में पेस्ट कर सकता है।',
		ar: 'لمسار ملف مصدر معين داخل مساحة العمل، يُرجع مسار spec المرافق الذي تفرضه الاتفاقية (يعتمد الموضع على `specLayout`) بالإضافة إلى هيكل `describe(...)` بسيط يمكن للوكيل لصقه كمحتوى للـ spec.',
		th: 'สำหรับพาธไฟล์ต้นทางที่กำหนดในเวิร์กสเปซ ส่งคืนพาธ spec companion ที่ข้อตกลงกำหนด (ตำแหน่งขึ้นอยู่กับ `specLayout`) พร้อมโครงร่าง `describe(...)` แบบขั้นต่ำที่เอเจนต์สามารถวางเป็นเนื้อหา spec ได้',
		vi: 'Đối với một đường dẫn tệp nguồn cho trước trong workspace, trả về đường dẫn spec đi kèm mà quy ước yêu cầu (vị trí phụ thuộc vào `specLayout`) cùng với một khung `describe(...)` tối thiểu mà agent có thể dán làm nội dung spec.',
	},
};
