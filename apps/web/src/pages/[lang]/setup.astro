---
/**
 * `/[lang]/setup` — localized setup page. See
 * `pages/[lang]/tools.astro` for the recipe.
 */
import Setup from '#COMPONENTS/Setup.astro';
import PageShell from '#COMPONENTS/PageShell.astro';
import { langStaticPaths, resolveLang, localePath } from '#LIB/page';
import { useTranslations } from '#I18N/ui';

export const getStaticPaths = langStaticPaths;

const lang = resolveLang(Astro.params.lang);
const t = useTranslations(lang);
---

<PageShell
	lang={lang}
	title={t.setup.title}
	base={import.meta.env.BASE_URL.replace(/\/$/, '')}
	crumbs={[{ label: t.nav.setup, href: localePath(lang, 'setup') }]}
>
	<Setup lang={lang} />
</PageShell>
