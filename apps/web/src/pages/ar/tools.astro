// REMOVED — this route is now served by [lang]/tools.astro (en + 11 locales).
// Kept as a placeholder so the file still exists in git history; Astro will
// ignore it because there is no frontmatter + no exported `getStaticPaths`
// that would mark it as a routable page. Remove the file in a follow-up
// cleanup commit.
