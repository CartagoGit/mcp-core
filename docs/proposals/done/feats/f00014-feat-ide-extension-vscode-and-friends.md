---
id: f00014
kind: feat
title: IDE extension (VS Code first, JetBrains/Zed later) — generic MCP client + tree + webviews
status: done
triaged: true
date: 2026-06-20
track: apps+client+docs
budget: { maxInputTokens: 80000, maxOutputTokens: 40000, maxIterations: 120 } # per slice-claim
ownership:
    - {
          agent: implementation_runner,
          task: 'S1-S3: workspace packages/client (MCP stdio client + OverviewService + NotificationsService + in-memory mock) with full unit tests',
      }
    - {
          agent: implementation_runner,
          task: 'S4-S7: apps/vscode scaffold + activate + registerTreeDataProvider + status bar + command palette wiring consuming packages/client',
      }
    - {
          agent: implementation_runner,
          task: 'S8-S10: tool-detail webview + proposals board view + metrics sparkline using generated tool-outputs types',
      }
    - {
          agent: implementation_runner,
          task: 'S11-S12: VSIX packaging + .vscodeignore + publisher config + i18n strings (en + es + ja + zh + de + fr minimum, mirroring apps/web/src/i18n/ui.ts)',
      }
reservedFiles:
    - packages/client/
    - apps/vscode/
    - apps/vscode/.vscodeignore
    - apps/vscode/package.json
    - apps/vscode/src/extension.ts
    - apps/vscode/src/providers/
    - apps/vscode/src/commands/
    - apps/vscode/src/views/
    - apps/vscode/src/test/
    - apps/vscode/src/i18n/
    - apps/vscode/README.md
    - apps/vscode/CHANGELOG.md
    - docs/IDE-EXTENSION.md
acceptance:
    - { command: bun run type, expect: exit0 }
    - { command: bun run test, expect: exit0 }
    - { command: bun run lint, expect: exit0 }
    - { command: bun run site:strict, expect: exit0 }
    - { command: bun run lint:proposals, expect: exit0 }
    - { command: cd apps/vscode && bun run package, expect: exit0 }
related:
    - f113 # proposal state machine — the extension reads its board
    - f100 # i18n baseline — extension mirrors the language list
    - f101 # capabilities surface — extension is the IDE counterpart
    - f110 # residual p100 web & i18n — same i18n rules apply
---

# f114 — IDE extension (VS Code first, JetBrains/Zed later)

## goal

Ship a **VS Code extension** that consumes the existing `@mcp-vertex/core`
MCP server through a **new, IDE-agnostic `@mcp-vertex/client` library**, so
the same client can later drive JetBrains, Zed, Cursor and Antigravity
without duplicating tool/plugin/notification logic. The extension surfaces
the live tool registry (`overview`), per-tool detail (`knowledge` +
generated `outputSchema`), the proposal board, agent locks and metrics —
all from the MCP server, with no host vocabulary leaking into the core.

## why

Three concrete pains today:

1. **Discoverability**: the registry is 50+ tools across 11 plugins. The
   only way to see them is to open the chat and call `mcp-vertex_overview`.
   There is no tree, no palette entry, no hover help.
2. **Operational state is invisible**: the proposals plugin already
   reports `proposal_board`, `agent_names`, `compact_status`, but nothing
   in the IDE surfaces them. The user has to remember a tool name to see
   if work is in flight.
3. **Multi-IDE waste risk**: when JetBrains support arrives, doing it as
   a second copy-paste of the same tool/proposal code is the textbook
   way to drift. Extracting the client first makes the second IDE 80%
   pure UI translation.

This proposal **does not** add a new plugin to the server. It is a
**client of the existing server**, sitting in `apps/` next to `apps/web`,
governed by the same i18n/lint/validate rules.

## non-goals

- **No new MCP plugin.** This is a pure client. New capabilities go
  through the normal proposals workflow.
- **No bundling of the server inside the extension.** The extension
  spawns the server via stdio; it does not embed it. Keeps the core's
  versioning independent.
- **No auth/secret storage** in v0.1. The server is local-only today;
  when auth lands, follow up in `f116`.
- **No codeLens over tool calls in user code** — punted to a future
  proposal (it needs the inverse index of which tools a repo uses,
  which the metrics plugin does not yet expose).

## architecture

### 2.1 Hard rules (cannot be broken)

- **`packages/core` stays agnostic.** No `vscode`, `cursor`, `jetbrains`
  or other host import may appear under `packages/core` or any plugin
  in `plugins/*`. Lint forbids it.
- **`packages/client` is the only MCP-aware code shared with IDE hosts.**
  It is pure TypeScript, zero `@vscode/*` imports, runs under `node`,
  `bun` and any modern bundler. This is what makes JetBrains/Zed possible.
- **`apps/vscode` may import `@mcp-vertex/core` and the plugins for
  _types only_ (interfaces generated by `bun run types:generate`).** It
  may NOT import runtime logic from `packages/core`. The runtime path
  is `stdio → MCP server → packages/client → UI`.
- **`apps/vscode` mirrors `apps/web`'s i18n surface.** Adding a string
  requires adding it for every language in `apps/web/src/i18n/ui.ts` or
  `apps/vscode/scripts/check-i18n.ts` will fail the build.
- **No `process.cwd()` in `packages/client`.** All paths are injected via
  options; tests pass an in-memory transport.

### 2.2 Data flow

```
                ┌────────────────────────┐
                │ @mcp-vertex/core       │
                │ (MCP server, stdio)    │
                └──────────┬─────────────┘
                           │ JSON-RPC over stdio
                           ▼
                ┌────────────────────────┐
                │ @mcp-vertex/client     │
                │ - McpStdioClient       │
                │ - OverviewService      │
                │ - NotificationsService │
                │ - MetricsService       │
                └──────────┬─────────────┘
                           │ typed JS objects
                           ▼
        ┌──────────────────┴───────────────────┐
        │                                      │
┌───────▼────────┐                  ┌──────────▼─────────┐
│ apps/vscode    │                  │ apps/jetbrains     │
│ (future)       │                  │                    │
└────────────────┘                  └────────────────────┘
```

### 2.3 Where types come from

`bun run types:generate` (already runs in CI) emits
`packages/core/src/generated/tool-outputs.ts` and one
`<Plugin>ToolOutputs.ts` per plugin with `export interface`s and a
`name → type` map. `packages/client` re-exports the **type** portion of
those modules so the IDE host gets:

```ts
import type { McpVertexToolOutputs } from '@mcp-vertex/client';
type Overview = McpVertexToolOutputs['mcp-vertex_overview'];
// ↑ fully typed; no JSON.parse → any.
```

Runtime values flow through the MCP client; types flow through
`generated/`. This is the same pattern `apps/web` already uses.

## slices

Each slice ends green (`bun run validate`), ships a Conventional
Commit, and updates this proposal's `shipped-in` list in `index.json`.

## acceptance

- `bun run type`, `bun run test`, `bun run lint`, `bun run site:strict`,
  `bun run lint:proposals` and `cd apps/vscode && bun run package` all
  exit 0 from the root.
- New unit tests in `packages/client/tests/` and `apps/vscode/src/test/`.
- A smoke e2e that spawns the real server, calls `overview`, asserts
  the JSON shape, exits 0.
- A `.vsix` artifact produced and uploaded as a workflow artifact in
  CI for manual install.

### S1 — `packages/client` scaffold + `McpStdioClient` _(excl. `packages/client/src/lib/services/`, `apps/`, `docs/`)_

- **Status**: done
- **Files**:
    - `packages/client/package.json`
    - `packages/client/src/lib/transport/mcp-stdio-client.ts`
    - `packages/client/src/lib/transport/mcp-transport.types.ts`
    - `packages/client/src/public/index.ts`
    - `packages/client/tests/transport/mcp-stdio-client.spec.ts`
    - `packages/client/tests/e2e/mcp-stdio-client.e2e.spec.ts`
- New workspace `packages/client/`.
- `package.json`: name `@mcp-vertex/client`, `type: "module"`, depends
  on `@mcp-vertex/core` (workspace:_), no `@vscode/_`.
- `mcp-stdio-client.ts`: spawns the server with `Bun.spawn` (fallback
  `node:child_process`), speaks MCP JSON-RPC over stdio, exposes
  `request<TIn, TOut>(tool: string, args: TIn): Promise<TOut>`.
- `mcp-transport.types.ts`: declares `IMcpTransport` (the seam used by
  unit tests).
- `src/public/index.ts`: re-exports the typed `McpVertexToolOutputs`
  (and per-plugin maps) from `packages/core/src/generated/`.
- Unit tests use an in-memory `IMcpTransport` mock; no real process
  spawn. The e2e spawns the real CLI.
- **Gate**: `bun run test packages/client` (coverage on
  `mcp-stdio-client.ts` ≥ 90%).
- **Estimated work**: 1 session.
- **Shipped**: `packages/client` scaffold, `McpStdioClient`,
  `IMcpTransport`, type re-exports, in-memory unit tests and stdio e2e.

### S2 — `OverviewService` + `KnowledgeService` _(excl. `apps/`, `docs/`)_

- **Status**: done
- **Files**:
    - `packages/client/src/lib/services/overview-service.ts`
    - `packages/client/src/lib/services/knowledge-service.ts`
    - `packages/client/src/lib/services/tool-descriptor.types.ts`
    - `packages/client/tests/services/overview-service.spec.ts`
    - `packages/client/tests/services/knowledge-service.spec.ts`
- `overview-service.ts`:
    - `getOverview(opts: { compact?: boolean; tag?: string }): Promise<McpVertexToolOutputs["mcp-vertex_overview"]>`
    - `listTools(): Promise<ToolDescriptor[]>`
- `knowledge-service.ts`:
    - `getKnowledge(id: string): Promise<KnowledgeEntry>`
- Types come from `McpVertexToolOutputs`. No `any`, no `unknown` in the
  public API.
- Tests with a fake transport that returns fixture JSON.
- **Gate**: `bun run test packages/client` exits 0.
- **Estimated work**: 1 session.
- **Shipped**: `OverviewService`, `KnowledgeService`, stable tool
  descriptor normalization, knowledge summaries/bodies and unit tests.

### S3 — `NotificationsService` + `MetricsService` _(excl. `apps/`, `docs/`)_

- **Status**: done
- **Files**:
    - `packages/client/src/lib/services/notifications-service.ts`
    - `packages/client/src/lib/services/metrics-service.ts`
    - `packages/client/tests/services/notifications-service.spec.ts`
    - `packages/client/tests/services/metrics-service.spec.ts`
- `notifications-service.ts` wraps `notification_notify_status` and
  `notification_await_lock`; exposes an `EventTarget`-style
  `addEventListener("lock-released" | "cap" | "bloqueado", cb)`.
- `metrics-service.ts` wraps `mcp-vertex_metrics`; exposes
  `snapshot()` and `stream(intervalMs)` returning typed
  `McpVertexToolOutputs["mcp-vertex_metrics"]`.
- Tests verify event dispatch + back-pressure (drops if subscriber slow).
- **Gate**: `bun run test packages/client` exits 0.
- **Estimated work**: 1 session.
- **Shipped**: notification status/await-lock wrappers, EventTarget-style
  listeners for `lock-released`, `cap`, `bloqueado`, slow-subscriber
  back-pressure, metrics snapshots and async metric streams.

### S4 — `apps/vscode` scaffold _(excl. `apps/vscode/src/providers/`, `apps/vscode/src/commands/`, `apps/vscode/src/views/`, `apps/vscode/src/i18n/`, `docs/`)_

- **Status**: done
- **Files**:
    - `apps/vscode/package.json`
    - `apps/vscode/tsconfig.json`
    - `apps/vscode/vitest.config.ts`
    - `apps/vscode/src/extension.ts`
    - `apps/vscode/src/test/smoke.spec.ts`
- New workspace `apps/vscode/` mirroring `apps/web/package.json` shape
  (depends on `@mcp-vertex/core` and selected plugins for types).
- `src/extension.ts`: `activate(ctx)` calls
  `McpStdioClient.connect({ command: "bun", args: ["run", "mcp-vertex"] })`
  and stores the client on `ctx.globalState`.
- `package.json#contributes`: empty `viewsContainers` + one command
  `mcp-vertex.showOverview` that opens a webview.
- Smoke test in `src/test/smoke.spec.ts` runs against the **vscode-test
  harness** (the in-memory mock client, no real VS Code).
- **Command**: `cd apps/vscode && bun run type && bun run test`.
- **Expect**: exit 0.
- **Estimated work**: 1 session.
- **Shipped**: `apps/vscode` manifest, TypeScript/Vitest config,
  injectable activation, `showOverview` webview command and smoke test.

### S5 — TreeView: Servers → Plugins → Tools _(excl. `apps/vscode/src/extension.ts`, `apps/vscode/src/views/`, `apps/vscode/src/commands/`, `docs/`)_

- **Status**: done
- **Files**:
    - `apps/vscode/src/providers/tool-tree-data-provider.ts`
    - `apps/vscode/src/providers/tool-tree-node.ts`
    - `apps/vscode/src/test/tool-tree-data-provider.spec.ts`
    - `apps/vscode/package.json` (updated `contributes.views`)
- `tool-tree-data-provider.ts`:
    - Root: the server (`mcp-vertex`).
    - Level 1: one node per plugin (icon by namespace).
    - Level 2: one node per tool with hover text = description from
      `knowledge`.
    - Refresh on `mcp-vertex.config.json` change via FS Watcher.
- Tests with a fake `OverviewService` returning a 3-plugin fixture.
- **Gate**: `bun run test apps/vscode` exits 0.
- **Estimated work**: 1 session.
- **Shipped**: tool tree nodes, provider, watcher-driven refresh,
  manifest view contribution and provider tests.

### S6 — Command palette wiring _(excl. `apps/vscode/src/providers/`, `apps/vscode/src/views/`, `apps/vscode/src/i18n/`, `docs/`)_

- **Status**: done
- **Files**:
    - `apps/vscode/src/commands/show-overview.ts`
    - `apps/vscode/src/commands/refresh.ts`
    - `apps/vscode/src/commands/run-validation.ts`
    - `apps/vscode/src/commands/open-proposal.ts`
    - `apps/vscode/src/extension.ts` (updated `registerCommand` block)
    - `apps/vscode/package.json` (updated `contributes.commands`)
    - `apps/vscode/src/test/commands.spec.ts`
- `mcp-vertex.showOverview` — opens the JSON in a webview.
- `mcp-vertex.refresh` — re-fetches `overview`.
- `mcp-vertex.runValidation` — calls
  `mcp-vertex_get_validation_matrix` + `quality_run_quality` (dry) and
  shows the result.
- `mcp-vertex.openProposal` — shows `proposals_proposal_board`.
- **Gate**: `bun run test apps/vscode` exits 0.
- **Estimated work**: 1 session.
- **Shipped**: command modules for overview, refresh, validation and
  proposal board; activation wiring; command tests.

### S7 — Status bar item _(excl. `apps/vscode/src/views/`, `apps/vscode/src/commands/`, `apps/vscode/src/i18n/`, `docs/`)_

- **Status**: done
- **Files**:
    - `apps/vscode/src/providers/status-bar.ts`
    - `apps/vscode/src/extension.ts` (registers StatusBar on activate)
    - `apps/vscode/src/test/status-bar.spec.ts`
- `status-bar.ts`: shows `$(mcp-vertex) mcp-vertex • N tools • P
proposals` with a click → `showOverview`. Updates on
  `NotificationsService` events.
- **Gate**: `bun run test apps/vscode` exits 0.
- **Estimated work**: 0.5 session.
- **Shipped**: status bar provider, activation wiring, notification-driven
  refresh and tests.

### S8 — Tool detail webview _(excl. `apps/vscode/src/extension.ts`, `apps/vscode/src/commands/`, `apps/vscode/src/i18n/`, `docs/`)_

- **Status**: done
- **Files**:
    - `apps/vscode/src/views/tool-detail-webview.ts`
    - `apps/vscode/src/views/render-output-schema.ts`
    - `apps/vscode/src/views/tool-detail.html`
    - `apps/vscode/src/views/tool-detail.css`
    - `apps/vscode/src/test/render-output-schema.spec.ts`
- Sidebar / editor webview showing: name, plugin, description, input
  schema (rendered), `outputSchema` (rendered from generated types),
  last 5 calls (latency from `MetricsService`).
- **Plain HTML + preact** (already used by `apps/web`; reuse its
  component scripts if possible). Do NOT pull in a full React/Vue
  runtime.
- Tests cover schema rendering with two fixtures.
- **Gate**: `bun run test apps/vscode` exits 0.
- **Estimated work**: 1.5 sessions.
- **Shipped**: schema renderer, tool detail HTML/CSS, metrics-aware detail
  renderer and rendering tests.

### S9 — Proposal board view _(excl. `apps/vscode/src/extension.ts`, `apps/vscode/src/commands/`, `apps/vscode/src/views/tool-detail_`, `apps/vscode/src/i18n/`, `docs/`)\*

- **Status**: done
- **Files**:
    - `apps/vscode/src/providers/proposal-board-provider.ts`
    - `apps/vscode/src/test/proposal-board-provider.spec.ts`
    - `apps/vscode/package.json` (second `contributes.views` entry)
- Second tree provider, sorted by status
  (in_progress > ready > blocked > paused > done).
- Each node clickable → opens the proposal `.md` in the editor with
  `vscode.workspace.openTextDocument`.
- **Gate**: `bun run test apps/vscode` exits 0.
- **Estimated work**: 1 session.
- **Shipped**: proposal board provider, status sorting, clickable command
  metadata and second manifest view.

### S10 — Metrics sparkline _(excl. `apps/vscode/src/views/tool-detail_`, `apps/vscode/src/i18n/`, `docs/`)\*

- **Status**: done
- **Files**:
    - `apps/vscode/src/views/metrics-sparkline.ts`
    - `apps/vscode/src/views/metrics-sparkline.html`
    - `apps/vscode/src/commands/show-metrics.ts`
    - `apps/vscode/src/test/metrics-sparkline.spec.ts`
- Webview that polls `MetricsService` every 5s; renders a tiny inline
  SVG (no chart library dep).
- A test verifies the SVG string for a given snapshot.
- **Gate**: `bun run test apps/vscode` exits 0.
- **Estimated work**: 1 session.
- **Shipped**: metrics sparkline renderer, metrics webview command,
  manifest command and tests.

### S11 — i18n + quality gate _(excl. `apps/web/`, `apps/vscode/src/extension.ts`, `apps/vscode/src/providers/`, `apps/vscode/src/views/`, `docs/`)_

- **Status**: done
- **Files**:
    - `apps/vscode/src/i18n/langs/en.ts`
    - `apps/vscode/src/i18n/langs/es.ts`
    - `apps/vscode/src/i18n/langs/ja.ts`
    - `apps/vscode/src/i18n/langs/zh.ts`
    - `apps/vscode/src/i18n/langs/de.ts`
    - `apps/vscode/src/i18n/langs/fr.ts`
    - `apps/vscode/src/i18n/index.ts`
    - `apps/vscode/scripts/check-i18n.ts`
    - `package.json` (root, adds `lint:ide` script)
- New `apps/vscode/src/i18n/langs/{en,es,ja,zh,de,fr}.ts` mirroring
  the language set in `apps/web/src/i18n/ui.ts`.
- `check-i18n.ts` mirroring `apps/web/scripts/check-i18n.ts`. Wired to
  `bun run lint` via `lint:ide` (runs both web and vscode checks).
- **Gate**: `bun run lint` exits 0 (includes the new `lint:ide`).
- **Estimated work**: 1 session.
- **Shipped**: 12-language extension i18n catalog, completeness checker,
  app script and root `lint:ide` integration.

### S12 — VSIX packaging + README _(excl. `apps/vscode/src/extension.ts`, `apps/vscode/src/providers/`, `apps/vscode/src/views/`, `apps/vscode/src/commands/`, `apps/vscode/src/i18n/`)_

- **Status**: done
- **Files**:
    - `apps/vscode/.vscodeignore`
    - `apps/vscode/package.json` (adds `vsce` script + publisher field)
    - `apps/vscode/README.md`
    - `apps/vscode/CHANGELOG.md`
    - `docs/IDE-EXTENSION.md`
    - `package.json` (root, adds `bun run package` workspace script)
- `.vscodeignore` excludes `src/test/`, `dist/test/`, fixtures.
- `bun run package` invokes `vsce package --no-git-tag-version`.
- `apps/vscode/README.md` documents: install, dev (`bun run watch`),
  package, debug, contribute.
- `docs/IDE-EXTENSION.md`: user-facing docs (what the extension shows,
  how it talks to the server, troubleshooting when stdio fails).
- Final commit: `feat(apps): vscode extension v0.1.0 ships generic
@mcp-vertex/client` so the monorepo can tag the release.
- **Command**: `cd apps/vscode && bun run package`.
- **Expect**: produces `apps/vscode/mcp-vertex-vscode-0.1.0.vsix`,
  exit 0.
- **Shipped**: VSIX packaging script, `.vscodeignore`, local license,
  README, changelog, root package script and IDE extension docs.

## risks and mitigations

| Risk                                             | Mitigation                                                                                                           |
| ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- |
| Stdio spawn differs across hosts (Win/Mac/Linux) | `McpStdioClient` uses `Bun.spawn`; CI matrix exercises all three.                                                    |
| `@vscode/test-electron` is heavy                 | Use `@vscode/test-cli` (the new lightweight runner) + mock client for unit tests.                                    |
| Generated types drift when a tool changes        | `bun run types:generate` is in CI; `apps/vscode` `bun run type` fails if regenerated types are not committed.        |
| i18n gate blocks the release                     | S11 is its own slice so it lands early; the `check-i18n` script is the canary.                                       |
| Cursor/Antigravity fork the VS Code API          | They share it; extension works out of the box. Documented in README.                                                 |
| JetBrains later                                  | Reserved: when S12 ships, file `f115-feat-jetbrains-extension.md` reusing `packages/client`. The client is the seam. |

## notes

- **Multi-IDE from day one**: `packages/client` is the seam. Cursor /
  Antigravity / Windsurf inherit VS Code support for free (they are
  VS Code forks). PhpStorm / WebStorm come later via a JetBrains plugin
  that consumes the same client.
- **Types-only imports from `@mcp-vertex/core`** in `apps/vscode`.
  Runtime goes through MCP.
- **Library, not framework**: `packages/client` exposes typed
  services; the host wires them. No DI container.
- **No React/Vue** in webviews: preact + plain DOM to keep the VSIX
  small.
