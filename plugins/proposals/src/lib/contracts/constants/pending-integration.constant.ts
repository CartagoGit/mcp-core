/**
 * pending-integration.constant.ts — f00091 S2.
 *
 * On-disk schema version for the pending-integration store (the branches
 * `close_slice` records as finished-but-unintegrated).
 */
export const PENDING_INTEGRATION_VERSION = 1;
